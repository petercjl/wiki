# Knowledge Unit Inventory

| id | knowledge unit | intended reuse |
| --- | --- | --- |
| KU01 | 从物理局域网切换到 Tailscale overlay，不改变上层 SSH/SCP 调用协议 | 网络演进模板 |
| KU02 | Tailscale 提供稳定虚拟地址、WireGuard 加密、NAT 穿透和 DERP 兜底 | 异地连接解释 |
| KU03 | Worker 的 Tailscale 与 sshd 应自动启动，且只开放必要端口 | Worker 基线 |
| KU04 | CLI 应封装上传、远端执行、下载、清理和统一错误语义 | 客户端接口 |
| KU05 | job_id 只能隔离文件，不能替代 GPU 调度队列 | 调度核心判断 |
| KU06 | Real-ESRGAN NCNN Vulkan + 模型映射构成超分执行器 | 案例实现 |
| KU07 | 2×/3× 先做 4× 再缩放是具体兼容补丁，不是通用原则 | 案例边界 |
| KU08 | 当前原型没有中央队列、配额、优先级、状态 API 和审计 | 成熟度判断 |
| KU09 | 面向公司不能共享管理员 SSH 私钥，应使用个人/服务身份和最小权限 | 安全原则 |
| KU10 | Tailscale 网络权限不等于任务权限；还需应用层鉴权和配额 | 双层授权 |
| KU11 | 单 GPU 默认需要受控并发；显存型任务通常从并发 1 开始压测 | 容量原则 |
| KU12 | 提交、排队、执行、完成、失败、取消、超时应成为显式状态机 | 平台状态模型 |
| KU13 | 必须记录任务人、模型、参数、耗时、显存、结果与降级状态 | 审计与可观测性 |
| KU14 | 输入输出需定义隔离、保留期、清理与敏感数据边界 | 数据治理 |
| KU15 | fallback 必须改变机器可读状态，不能只打印警告 | 防假成功 |
| KU16 | 可按 CLI 原型、单 Worker 队列、公司服务、多 Worker 资源池四阶段演进 | 落地路线 |

